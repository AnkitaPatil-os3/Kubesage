apiVersion: v1
kind: Service
metadata:
  name: postgres-k8sgpt
  labels:
    app: postgres-k8sgpt
spec:
  ports:
  - port: 5432
    targetPort: 5432
  selector:
    app: postgres-k8sgpt
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-k8sgpt
spec:
  selector:
    matchLabels:
      app: postgres-k8sgpt
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: postgres-k8sgpt
    spec:
      containers:
      - image: postgres:14-alpine
        name: postgres
        env:
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: k8sgpt-db-credentials
              key: username
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: k8sgpt-db-credentials
              key: password
        - name: POSTGRES_DB
          value: k8sgpt_db
        ports:
        - containerPort: 5432
          name: postgres
        volumeMounts:
        - name: postgres-data
          mountPath: /var/lib/postgresql/data
      volumes:
      - name: postgres-data
        persistentVolumeClaim:
          claimName: postgres-k8sgpt-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-k8sgpt-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard