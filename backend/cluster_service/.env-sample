# Cluster Service Environment Configuration

# Service Configuration
HOST=0.0.0.0
PORT=8007
DEBUG=true

# User Service Configuration (for authentication)
USER_SERVICE_URL=https://10.0.32.106:8001

# RabbitMQ Configuration
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest
RABBITMQ_VHOST=/

# WebSocket Configuration
WEBSOCKET_TIMEOUT=30

# Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS_PER_MINUTE=60

# SSL Configuration (set USE_SSL=true for production)
SSL_KEYFILE=key.pem
SSL_CERTFILE=cert.pem
USE_SSL=false
